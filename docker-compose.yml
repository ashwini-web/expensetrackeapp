
version: "3.8"

services:
  backend:
    image: asrathod/expense-backend:2.0
    ports:
      - "8080:8080"

  frontend:
    image: asrathod/expense-frontend:2.1
    ports:
      - "3000:80"
    depends_on:
      - backend
