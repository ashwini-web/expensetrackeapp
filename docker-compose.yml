
version: "3.8"

services:
  backend:
    image: asrathod/expense-backend:latest
    ports:
      - "8080:8080"

  frontend:
    image: asrathod/expense-frontend:latest
    ports:
      - "3000:80"
    depends_on:
      - backend
